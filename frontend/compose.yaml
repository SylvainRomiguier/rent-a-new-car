services:
  rental-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    container_name: rental-frontend
    restart: unless-stopped
    networks:
      - app-network
networks:
  app-network:
